const test=(dropdownItem)=>{
    //let item=document.querySelector(".dropdown-item").textContent;
    let dugme=document.getElementById("kategorija");
    dugme.textContent=dropdownItem.textContent;
}